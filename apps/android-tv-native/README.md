# Euripus Android TV Receiver

This module is the starting point for the dedicated native Android TV receiver app.

Scope for v1:

- launch directly into receiver mode
- create a receiver session against the Euripus API
- show a pairing code
- receive playback and transport commands
- play relay-backed streams with Media3 / ExoPlayer

The initial implementation scaffold lives under `app/src/main/`.
